# Gaby Herbstein project notes

## Purpose
Preserve the original Gaby Herbstein website structure and content while keeping the repository easy to maintain.

## Structure
- `index.html` for the public site entry point
- `404.html` for the fallback page
- `robots.txt` and `sitemap.xml` for discoverability
- `vercel.json` for deployment settings

## Maintenance
- Keep the root README short and stable.
- Add deeper operational notes here when the site changes.
